# Custom CodeQL query configuration
# Teaches CodeQL about our custom rate limiting middleware

# This file informs CodeQL that createRateLimiter is a rate-limiting function
# and should be recognized as such in security analysis

# The createRateLimiter function in server/index.js implements rate limiting
# using the express-rate-limit library pattern
